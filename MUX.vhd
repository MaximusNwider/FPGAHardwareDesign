Library IEEE;
Use IEEE.std_logic_1164.All;

ENTITY MUX is
GENERIC(
N: INTEGER := 1;
);
PORT(
A : IN STD_LOGIC;
B : IN STD_LOGIC;
S : IN STD_LOGIC;

G : OUT STD_LOGIC

);
END ENTITY;

ARCHITECTURE BEHAVIORAL OF MUX IS

begin

G <= (A AND NOT(S)) AND (B AND S);

END BEHAVIORAL;